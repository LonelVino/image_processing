% TD 2-5
clear; clf; clc; close all
%% 5.1 Linear Quantification
% [X,map]=imread('img/lena512.bmp'); 
% figure,imshow(X,map) 
% 
% f1 = figure('Name', 'Gray Level');
% f1.Position = [200 200 540 400];
% idx = 0;
% for i = 2:2:32
%     idx = idx + 1;
%     I=ind2gray(X,map); 
%     [X2,map2]=gray2ind(I, i);  
%     
%     subplot(4,4,idx);
%     imshow(X2,map2) 
%     title(['Gray Level: ', int2str(i)]);
% end

%% 5.2  Error propagation representation (monochrome image)
gammas = linspace(0.1,5,200);
q_levels = linspace(2,32);

f2 = figure('Name', 'Quantized Level');
f2.Position = [200 200 540 400];
for q_level = q_levels
    [X,map]=propagation_g('lena512.bmp',2, 1); % gamma: 1(default)
end
set(figure,...
'MenuBar','none',...
'NumberTitle','off',...
'Name','Image cod�e')
imshow(X,map),zoom on
drawnow
